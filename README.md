# 🎯 Flutter Quiz App

A simple **Flutter Quiz Application** built to demonstrate Flutter UI, navigation, state management, and quiz logic.  
This app allows users to answer multiple-choice questions with scoring and result display.

---

## 🧠 Features

- 📱 Clean and responsive UI  
- ❓ Multiple choice quiz questions  
- 🧮 Score counting and result display  
- 🔁 Easy to extend with more questions  
- 💡 Great starter project for Flutter learning

---

## 🛠️ Technologies Used

- **Flutter** — Cross-platform UI toolkit by Google  
- **Dart** — Programming language for Flutter apps  
- Flutter widgets for UI and state management  
- Pub package dependencies as defined in `pubspec.yaml`

---

## Project Structure

├── android \
├── ios \
├── lib \
│ ├── main.dart \
│ └── (other dart files) \
├── test \
├── web \
├── pubspec.yaml \
├── .gitignore \
└── README.md \
---

## 🚀 Installation & Setup

### 1. Clone the repository
```bash
git clone https://github.com/Amar1925/flutter.git
cd flutter
2. Install Flutter (if not already)
Follow official docs: https://flutter.dev/docs/get-started/install
```
## ▶️ Run the App
## 🟢 Android / iOS / Web

Ensure a device is connected or an emulator is running:

```bash
flutter run
```


To run on a specific device:
```bash
flutter run -d chrome        # For Web
```
```bash
flutter run -d android       # For Android
```
```bash
flutter run -d ios           # For iOS
```

